import { Box, Typography } from "@mui/material";
const MarkerTab = ({ markers, setMarkers }) => {
  return (
    <Box>
      {/* Add your polygon adding UI here */}
      <Typography variant="h6">Marker 1</Typography>
      {/* Insert coordinates fields and save button UI */}
    </Box>
  );
};

export default MarkerTab;
